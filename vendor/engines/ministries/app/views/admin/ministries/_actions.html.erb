<ul>
  <% if Admin::MinistriesController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_ministries_url
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_ministry_url,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::MinistriesController.sortable? and Ministry.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Ministries"),
                 admin_ministries_url,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Ministries"),
                 admin_ministries_url,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>