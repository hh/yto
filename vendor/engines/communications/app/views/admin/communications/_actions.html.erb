<ul>
  <% if Admin::CommunicationsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_communications_url
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_communication_url,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::CommunicationsController.sortable? and Communication.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Communications"),
                 admin_communications_url,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Communications"),
                 admin_communications_url,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>